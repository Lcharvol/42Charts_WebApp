import React from 'react';

import { Container } from './styles';

const ProjectValidationGraph = ({ successRate }) => (
  <Container successRate={successRate} />
);

export default ProjectValidationGraph;
